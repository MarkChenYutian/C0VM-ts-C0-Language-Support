# Multiple Function Calls

int main() {
    printf("something");
    for(int i = 0; i < 100; i++)
    {
        printf("something_else");
    }
}

==>

Program(
    Function(
        Type, Identifier, CommaSep(),
        Scope(
            Expression()
        )
    )
)
